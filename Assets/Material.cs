using UnityEngine;

public class Material : MonoBehaviour
{
    internal readonly object color;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
